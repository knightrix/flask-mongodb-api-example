# Simple CRUD RESTful api using Flask and MongoDB

This is an example app that provide RESTful api to create/retrieve/update/delete menu items stored in MongoDB.

Requirements:
* MongoDB
* Flask-PyMongo
